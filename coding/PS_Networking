

#this is comment in powershell
#get-help get-aduser -showwindow

#ping host_Name -t
#ping host_name -w 4000

#test-netConnection host_name
#test-netConnection host_name -port 3389
#test-connection host_name

#nslookup (then press <enter> then type `help`)

#netstat -n
#netstat -anob
#netsh int ipv4 set dynamicport tcp start <start_port> num=<number_of_ports>

#gpupdate
#gpupdate /force

#get-aduser -identity "1631037891a" -properties *
#get-aduser -filter {samaccountname -like "yelo *"} | select-object name
